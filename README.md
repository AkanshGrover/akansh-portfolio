# Akansh's Portfolio Website

A responsive portfolio website built using React, HTML, CSS and JavaScript.
Projects are automatically fetched from GitHub, so the project section always stays up to date and never requires manual changes.